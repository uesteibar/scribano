---
- host: "amqp://guest:guest@localhost"
  exchange: "/my-exchange"
  routing_key: "#"
- host: "amqp://guest:guest@localhost"
  exchange: "/direct-exchange"
  exchange_type: "direct" # overwrite default type (topic)
  routing_key: "key.*"
